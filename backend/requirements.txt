fastapi[standard] # [standard] pulls in fastapi-cli, needed by `fastapi run` on FastAPI Cloud
uvicorn[standard]
sqlalchemy # the ORM
psycopg2-binary # the Postgres driver
python-dotenv # load .env variables
boto3==1.43.56
python-jose[cryptography] # JWT encode/decode for auth
bcrypt # password hashing
email-validator # backs pydantic's EmailStr
fpdf2 # knowledge/compare_rag.py renders the RAG-vs-base report to PDF
